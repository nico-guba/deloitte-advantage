version: 1
env:
  variables:
    UI_BASE: layers/presentation/ui
backend:
  phases:
    preBuild:
      commands: []
    build:
      commands: []
    postBuild:
      commands: []
frontend:
  phases:
    preBuild:
      commands:
        - cd ${UI_BASE}
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: ${UI_BASE}/build/output
    files:
      - '**/*'
test:
  phases:
    preTest:
      commands:
        - cd ${UI_BASE}
    test:
      commands:
        - npm run ci-test